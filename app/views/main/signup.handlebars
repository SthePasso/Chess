<form id="form-signup">
    <fieldset>
        <legend>Criar Conta</legend>

        <div class="form-group">
            <label for="nome">Nome Completo</label>
            <input type="text" class="form-control" name="nome" id="nome" value="{{user.nome}}" placeholder="Digite seu nome completo">
            <div class="invalid-feedback">O nome tem que ter entre 3 a 100 caracteres</div>
        </div>

        <div class="form-group">
            <label for="email">Endereço de Email</label>
            <input type="email" class="form-control" name="email" id="email" value="{{user.email}}" aria-describedby="emailHelp" placeholder="name@example.com">
            <div class="invalid-feedback">O email deve conter um domínio válido</div>
        </div>

        <div class="form-group">
            <label for="senha">Senha de Acesso</label>
            <input type="password" class="form-control" id="senha" placeholder="&bull;&bull;&bull;&bull;&bull;&bull;&bull;&bull;">
            <small id="fileHelp" class="form-text text-muted">A senha precisa ter 6 caracteres ou mais</small>
            <div class="invalid-feedback">A senha tem menos de 6 carasteres</div>
        </div>

        <div class="form-group">
            <label for="conf">Confirmar Senha de Acesso</label>
            <input type="password" class="form-control" id="conf" value="{{user.nome}}" aria-describedby="emailHelp" placeholder="&bull;&bull;&bull;&bull;&bull;&bull;&bull;&bull;">
            <div class="invalid-feedback">Senha incorreta.</div>
        </div>

        <input type="hidden" name="_csrf" value="{{csrf}}">
        <button type="submit" id="enviar" class="btn btn-primary">Criar Conta</button>
    </fieldset>
</form>